import React from 'react';

class Header extends React.Component {
  render() {
    return (
      <header className="movie-card-header">
        <h1 className="page-title">Movie Cards Library</h1>
      </header>
    );
  }
}

export default Header;

//
// Nesse arquivo foi criado um componente chamado 'Header' com a className="movie-card-header", tendo como filho uma tag 'h1', com a classeName="page-title"
